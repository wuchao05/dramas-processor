# 用户配置 - xx
# 此文件包含用户专属配置，会覆盖 default.yaml 中的对应字段

# 素材生成设置
count: 5 # 每部剧生成素材数量
material_code: "xx" # 素材文件名标识

# 标题颜色 - 绿色系
title_colors:
  - "#32CD32" # Lime Green
  - "#00FA9A" # Medium Spring Green
  - "#7CFC00" # Lawn Green
  - "#98FB98" # Pale Green
  - "#3CB371" # Medium Sea Green
  - "#00FF7F" # Spring Green

# 目录设置
default_source_dir: "/mnt/e/短剧剪辑/源素材视频" # WSL 路径

# 多素材文案配置
brand_text_mapping:
  mode: "range"
  ranges:
    - range: "01-05"
      text: "葸辉看剧"
  cycle_texts:
    - "葸辉看剧"
  default_text: "热门短剧"

# 飞书功能配置（核心字段）
enable_feishu_features: true  # 飞书功能总开关
feishu:
  app_id: "cli_a870f7611b7b1013"
  app_secret: "NTwHbZG8rpOQyMEnXGPV6cNQ84KEqE8z"
  app_token: "WdWvbGUXXaokk8sAS94c00IZnsf"
  table_id: "tblZ46PiVcROlbnu"

# 其他飞书配置（webhook、轮询、详细参数等）已在 default.yaml 中设置默认值
# 如需关闭飞书轮询，可覆盖: feishu_watcher: { enabled: false }
